# Orchestration — Zelda (PDF/AcroForm Specialist)

**Date:** 2026-05-18T11:35:10Z  
**Session:** PDF-specific exploit-surface review  
**Mode:** Background  

## Inputs Reviewed

- `src/PDFFlatten/**/*.cs` (flattening engine, appearance handling)
- `tests/PDFFlatten.Tests/*.cs` (regression coverage)

## Findings

**Signature handling deficiency:**
- Signed widgets (`/FT /Sig`) were not explicitly rejected, creating a subtle safety gap: replaying a signature appearance while removing the AcroForm preserves the visual mark but destroys verification semantics.
- This represents a security-sensitive downgrade, not a valid "flattened" output.

**Residual non-sanitizer risks:**
- `FlateDecode` inflation during appearance stream inspection can cause resource exhaustion if length claims exceed actual data.
- Non-widget active content (JavaScript, launch actions, embedded files) is not in scope; PDFFlatten is a rendering transform, not a sanitizer.

## Decisions Logged

1. **Zelda (Signature Boundary):** `/FT /Sig` now rejected fail-closed with `NotSupportedException`. Guard implemented and tested with synthetic classic-xref fixture.
2. **Impa (Security Posture):** Documented narrowly-scoped fail-closed posture; next hardening priorities documented for resource limits.

## Implementation Status

✅ Signature widget guard committed to `src/PDFFlatten/PdfFlattener.cs`  
✅ Synthetic test fixture added to `tests/PDFFlatten.Tests/ParserHardeningTests.cs`  
✅ All 53 existing tests passing  

## Next Actions

- Monitor for resource-exhaustion hardening in v0.4.0+
- No scope broadening for sanitization (out of bounds per charter)

## Status

**Completed.** Findings logged. Production posture remains: narrow fail-closed utility, not broad PDF sanitizer.
