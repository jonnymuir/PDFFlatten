# Orchestration — Impa (Lead)

**Date:** 2026-05-18T11:35:10Z  
**Session:** Security/architecture review of attack surface and residual risk  
**Mode:** Background  

## Inputs Reviewed

- `src/PDFFlatten/**/*.cs` (production code)
- `tests/PDFFlatten.Tests/*.cs` (test coverage)

## Findings

**Medium-risk residual vectors:**
- Resource-exhaustion via unbounded memory work: `/Length`-driven allocations, `FlateDecode` inflation during appearance inspection, full-input buffering without limits.
- Exception-contract gaps: Parser overflow paths emit raw exceptions instead of documented `NotSupportedException`/`InvalidOperationException`.
- Security posture messaging: PDFFlatten is a fail-closed AcroForm flattener, not a PDF sanitizer; non-widget active content not in scope.

## Decisions Logged

1. **Impa (Security Posture):** Documented fail-closed posture, next hardening priorities (resource limits, exception normalization).
2. **Zelda (Signature Boundary):** `/FT /Sig` widgets rejected fail-closed; replaying signature appearance while destroying verification semantics is unacceptable.

## Next Actions

- Prioritize resource-limit hardening for v0.4.0+
- Normalize exception handling across parser entry points
- No scope broadening for this cycle

## Status

**Completed.** Findings logged to `.squad/decisions.md`. No blockers for current release posture.
